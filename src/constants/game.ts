import { Location } from "./types";

export const NAME = "TS RSPS";

export const DEFAULT_LOCATION: Location = {
  x: 3250,
  y: 3423,
  z: 0,
};

export const DEFAULT_PORT = 43595;

export const CYCLE_RATE = 600;

// Not sure if this is needed... But keeping for now :)
export const GAME_UID = 8784521;
